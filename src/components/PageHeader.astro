---
interface Props {
  title: string;
  intro?: string;
}
const { title, intro } = Astro.props;
---

<header class="mb-10" data-reveal>
  <h1 class="text-3xl font-semibold tracking-tight">{title}</h1>
  {intro && <p class="mt-3 max-w-2xl text-muted">{intro}</p>}
</header>
